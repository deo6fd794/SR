name: blank

on:
  push:

jobs:
  expressiveness:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        setting: ["double", "arrive", "flash", "reality", "naked"]
        utilize: ["detect", "fleet", "specimen", "hate"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar vo
